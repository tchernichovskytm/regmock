<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="regmock.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Views="clr-namespace:regmock.Views"
    xmlns:helper="clr-namespace:regmock.Helper"
    Title="Mockflow Beta 1.0"
    Shell.FlyoutBehavior="Flyout">

    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate Views:RegisterPage}"
        IsVisible="{Binding IsNotLoggedIn}"
        Route="RegisterPage">
        <ShellContent.Icon>
            <FontImageSource
                FontFamily="MaterialIcons"
                Glyph="{x:Static helper:IconFont.App_registration}"
                Color="Black" />
        </ShellContent.Icon>
    </ShellContent>
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate Views:LoginPage}"
        IsVisible="{Binding IsNotLoggedIn}"
        Route="LoginPage">
        <ShellContent.Icon>
            <FontImageSource
                FontFamily="MaterialIcons"
                Glyph="{x:Static helper:IconFont.Login}"
                Color="Black" />
        </ShellContent.Icon>

    </ShellContent>

    <!--  DEBUG  -->
    <!--  DEBUG  -->

    <ShellContent
        Title="Give Help"
        ContentTemplate="{DataTemplate Views:GiveHelpPage}"
        IsVisible="{Binding IsLoggedIn}"
        Route="GiveHelpPage" />
    <ShellContent
        Title="Request Help"
        ContentTemplate="{DataTemplate Views:RequestHelpPage}"
        IsVisible="{Binding IsLoggedIn}"
        Route="RequestHelpPage" />
    <ShellContent
        Title="Pick Favorites"
        ContentTemplate="{DataTemplate Views:PickFavoritesPage}"
        IsVisible="{Binding IsLoggedIn}"
        Route="PickFavoritesPage" />


    <!--<ShellContent
        Title="New Preference"
        ContentTemplate="{DataTemplate Views:NewPreferencePage}"
        IsVisible="False"
        Route="NewPreferencePage" />
    <ShellContent
        Title="Edit Preference"
        ContentTemplate="{DataTemplate Views:EditPreferencePage}"
        IsVisible="False"
        Route="EditPreferencePage" />
    <ShellContent
        Title="New Ticket"
        ContentTemplate="{DataTemplate Views:NewTicketPage}"
        IsVisible="False"
        Route="NewTicketPage" />-->

</Shell>
